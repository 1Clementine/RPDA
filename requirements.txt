# RPDA (Ghost) 论文复现依赖 — 2026-08-15 冻结环境实测版本
#
# 环境基线：Python 3.8 + CUDA 11.7（RTX 3090 实测）
#
# torch 的 +cu117 变体来自 PyTorch 官方 wheel 源，需单独安装：
#     pip install torch==2.0.1+cu117 --extra-index-url https://download.pytorch.org/whl/cu117
# 其余依赖：
#     pip install -r requirements.txt

torch==2.0.1+cu117
transformers==4.35.2
numpy==1.23.5
pandas==2.0.3
scikit-learn==1.3.2
scipy==1.10.1
datasets==2.14.4

# 关键传递依赖（冻结环境实测版本；如遇依赖解析冲突可取消注释固定）
# tokenizers==0.15.0
# safetensors==0.3.1
# huggingface_hub==0.19.3
