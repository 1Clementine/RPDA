# RPDA 实验口径与结果冻结规范

## 1. 冻结状态

当前 RPDA 实验进入论文写作冻结阶段。

冻结范围包括：

- 已生成的模型检查点；
- GPT-2 主实验；
- 阶段性输出恢复实验；
- 逐层 REEF；
- ED-BA；
- BPB ratio；
- KL；
- Pythia-410M 辅助实验；
- 非同源模型参照；
- Ghost 静态谱实验；
- 已确定的表格和图中数值。

除非用户明确解除冻结，否则 Agent 只能修改：

- 论文结构；
- 术语；
- 表述方式；
- 表格布局；
- 图注；
- 数据解释；
- 引言、摘要和结论中的结论强度。

Agent 不得修改、重算或补充实验数值。


## 2. 唯一数据来源

冻结实验清单的唯一事实来源为：

`results/audit_final/experiment_freeze.csv`

论文正文中已经确认的冻结表格，可以作为展示层面的直接来源，但不得反向覆盖 audit 文件。

当正文、旧报告和 audit 文件冲突时，优先级为：

1. `results/audit_final/experiment_freeze.csv`
2. 已标记为 canonical/final 的结果文件
3. 当前冻结论文表格
4. 历史报告和旧版图表

若高优先级文件之间仍有冲突，停止修改并报告，不得自行消解。


## 3. REEF 的统一口径

### 3.1 REEF 实现

论文统一表述为：

“本文采用 REEF 的官方表示提取和 linear CKA 实现，并使用固定层比较协议。”

不得将本文自行定义的层平均指标称为“REEF 官方指标”或“官方 REEF 分数”。

### 3.2 GPT-2 主实验

GPT-2 主实验采用固定层、同深度比较。

主实验中使用三种展示方式：

1. `REEF-L6--11 Mean`
   - 用于概括 GPT-2 中后层轨迹；
   - 是本文定义的汇总指标；
   - 不是 REEF 原文定义的默认单一分数。

2. `REEF-L9`
   - 用作代表性固定层结果；
   - 用于恢复节点配对和逐层分析；
   - 不执行跨层搜索。

3. 逐层 REEF
   - 用于分析路径变化随模型深度的传播；
   - 不压缩为新的攻击成功阈值。

正文不得将 L6--L11 Mean、L9 和跨模型 CKA 混写为同一个指标。


### 3.3 跨模型参照协议

跨模型参照是独立的辅助协议，统一称为：

“等价深度固定层 cross-model CKA”

或：

“跨模型等价深度 CKA”。

它只用于校准表示变化幅度，不用于设定统一来源阈值。

层对应规则为：

- GPT-2：L9；
- GPT-Neo-125M：L9；
- 24 层模型：L18。

跨模型数值不得直接替代 GPT-2 主实验中的逐层 REEF 或 L6--L11 Mean。

特别注意：

- GPT-2 主实验逐层表中的 RPDA-C-r30 L9 数值；
- 跨模型辅助协议中的 RPDA-C-r30 cross-model CKA 数值；

必须保留在各自协议命名空间中。

在没有解除冻结并重新核验之前，不得声称二者是同一个数值，也不得用其中一个覆盖另一个。


### 3.4 Row-best 和跨层搜索

Row-best、global-best 或跨层最大 CKA 不属于正文主协议。

如已有结果，可仅作为附录中的强审计者分析，用于检查表示是否仅迁移至其他层。

不得：

- 用 row-best 替代固定层主结果；
- 将 row-best 与固定层非同源参照直接比较；
- 将跨层最大值称为 REEF 官方默认分数。


## 4. 当前跨模型结论口径

冻结的非同源参照结果为：

- GPT-2 Source：1.000；
- RPDA-C-r30：0.395；
- GPT-Neo-125M：0.391；
- Pythia-410M：0.332；
- Qwen2.5-0.5B：0.283；
- OPT-350M：0.278。

正文允许使用的表述：

“RPDA-C-r30 的跨模型 CKA 降至 0.395，与所选非同源模型中相似度最高的 GPT-Neo-125M（0.391）仅相差 0.004。”

或者：

“RPDA-C-r30 的固定深度表示相似性已降低至与最接近源模型的非同源参照近似的水平。”

禁止使用：

- “低于大部分非同源模型”；
- “低于全部非同源模型”；
- “已经进入非同源模型区间”；
- “达到非同源判定阈值”。

历史版本中关于 RPDA 恢复模型低于多个非同源模型的结果，属于旧辅助协议，不得用于描述当前冻结的固定层跨模型协议。


## 5. 模型检查点命名

统一采用：

- `RPDA-C-rX`：连续路径重组轨迹；
- `RPDA-S-rX`：此前已有恢复历史、但当前节点尚未恢复；
- `RPDA-S-rX(rec)`：当前节点完成恢复。

第一次恢复前使用：

- `RPDA-C-r16`

第一次恢复后使用：

- `RPDA-S-r16(rec)`

不存在：

- `RPDA-S-r16`

论文中 `(rec)` 前不加空格。


## 6. ED-BA 口径

ED-BA 为 EasyDetector 的平衡准确率版本。

正文中统一解释为：

“ED-BA 衡量源模型上线性探针向待审模型固定层表示的迁移能力。”

五个随机种子使用样本标准差。

为了保持主表简洁：

- 正文主表可以报告均值；
- 完整的 mean ± sample standard deviation 放入附录；
- 不得声称主表已经报告标准差，除非表中确实包含标准差。

ED-BA 后期接近 0.5 时，统一写为：

“总体降低并在接近二分类随机水平后出现平台或波动。”

禁止写：

“ED-BA 随轮次持续单调下降。”


## 7. 输出侧指标

输出侧状态由以下指标分别衡量：

- BPB ratio；
- next-token KL。

两者不得合并成未经定义的综合性能分数。

允许表述：

- “输出侧偏移得到改善”；
- “语言建模损失和输出分布差异减小”；
- “输出侧代价保持在相对受控范围”。

慎用：

- “模型能力完全恢复”；
- “模型性能恢复到源模型”；
- “模型可用性没有损失”。

除非有相应任务评测，不得将 BPB ratio 和 KL 扩展解释为所有下游能力。


## 8. Ghost 命名与解释

论文中的正式方法名称统一使用：

`Ghost`

不得在论文正文中替换为：

`GhostSpec`

`GhostSpec` 仅是源代码、脚本或结果字段中的内部命名。

正文指标统一称为：

- `Ghost correlation`
- 或“Ghost 静态谱相关性”。

冻结结果包括：

- Source：1.000000；
- RPDA-C-r30：0.999344；
- RPDA-S-r16(rec)：0.985303；
- RPDA-S-r30(rec)：0.926785。

统一结论为：

“纯路径重组对 Ghost 静态谱信号影响很小，而阶段性输出恢复更新了模型尾部权重，因此会被 Ghost 感知。”

禁止笼统写：

“RPDA 对 Ghost 始终没有影响。”

也禁止写：

“Ghost 被 RPDA 成功规避。”


## 9. Pythia-410M 口径

Pythia-410M 是跨架构辅助实验，不与 GPT-2 主实验视为完全等价。

允许结论：

- 路径重组在另一种残差架构中同样引起深度相关表示偏移；
- Pythia 对路径重组表现出更高输出侧敏感性；
- r02 输出恢复改善 BPB ratio 和 KL，但深层 REEF 基本未同步恢复。

禁止结论：

- Pythia 完整复现了 GPT-2 的全部阶段性轨迹；
- RPDA 在所有架构中具有相同代价；
- Pythia 结果证明方法具有普遍性。


## 10. 论文结论强度

推荐使用：

- “结果表明”；
- “结果支持”；
- “实验中观察到”；
- “暴露了”；
- “提供了证据”；
- “在本文设置下”。

避免使用：

- “证明”；
- “完全失效”；
- “普遍适用”；
- “彻底规避”；
- “对所有模型有效”。


## 11. 冲突处理规则

发现以下情况时，Agent 必须停止自动修改并报告：

- 同一模型、同一层、同一协议出现两个数值；
- 图中数值与表中数值不一致；
- 文件名显示 C 轨迹，但模型路径指向 S 轨迹；
- 同一指标混用 TQA200、TQA500 或其他审计集；
- same-depth 与 row-best 混用；
- Ghost 和 GhostSpec 被当作两种不同论文方法；
- 未经评估的模型被加入非同源参照；
- 缺失值被推测补齐。

报告格式应包含：

1. 冲突模型；
2. 冲突指标；
3. 两个数值；
4. 各自文件路径；
5. 各自协议；
6. 对论文结论可能造成的影响。

Agent 不得自行选择“更合理”或“结果更强”的数值。