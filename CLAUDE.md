## RPDA 实验结果冻结规则

在修改论文、表格、图注、实验分析、摘要、引言或结论之前，必须先完整阅读：

- `EXPERIMENT_FREEZE.md`
- `results/audit_final/experiment_freeze.csv`

上述文件是当前论文实验结果和评价口径的唯一事实来源。

除非用户明确下达“解除实验冻结”或“重新运行某项实验”的指令，否则禁止：

1. 重新运行实验；
2. 重新计算已有指标；
3. 使用其他脚本输出替换冻结结果；
4. 根据相邻检查点插值、推测或补齐缺失值；
5. 混用不同评价协议下的数值；
6. 为获得更强结论而重新选择层、检查点或模型；
7. 修改已有图表中的冻结数值；
8. 将辅助协议结果表述为主协议结果。

若不同文件或正文中出现数值冲突，不得自行选择一个结果，也不得静默覆盖。必须指出冲突涉及的模型、指标、协议和文件位置，并等待用户决定。

论文中的方法名称统一使用 `Ghost`。`GhostSpec` 仅为部分源代码或结果字段名称，不得替换论文方法名称。